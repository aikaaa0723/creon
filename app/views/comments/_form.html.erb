<%= form_with(model: [tweet, Comment.new], local: true) do |form| %>
  <div>
    <%= form.text_area :content, placeholder: "コメントを書く" %>
  </div>
  <div>
    <%= form.submit "送信" %>
  </div>
<% end %>
